
function Unit(position, team, type) {
  this.position = position;
  this.team = team;
  this.type = type;
}

Unit.prototype.move = function(direction) {
  let x = direction[0];
  let y = direction[1];

  
};